@{
    ViewBag.Title = "Home Page";
}
@*index*@
<main style="color:black">
    <h2>Danh sách vòng quay</h2>
    <ul>
        @foreach (var item in Model)
            {
                <li>
                    @item.Id - @item.NameLuckyDraw
                </li>
            }
    </ul>
</main>
